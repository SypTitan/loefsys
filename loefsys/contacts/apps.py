"""Module containing the app config for the contacts app."""

from django.apps import AppConfig


class ContactsConfig(AppConfig):
    """Config for the contacts app."""

    name = "loefsys.contacts"
