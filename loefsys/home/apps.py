"""Module defining the app configuration for the home app."""

from django.apps import AppConfig


class HomeConfig(AppConfig):
    """Configuration class for the home app."""

    name = "loefsys.home"
