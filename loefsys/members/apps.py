"""Module containing the app definition for the members app."""

from django.apps import AppConfig


class MembersConfig(AppConfig):
    """Configuration for the members app."""

    name = "loefsys.members"
    label = "members"
