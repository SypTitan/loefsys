"""Module defining the app configuration for the indexpage app."""

from django.apps import AppConfig


class IndexpageConfig(AppConfig):
    """Configuration class for the indexpage app."""

    name = "loefsys.indexpage"
